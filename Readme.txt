To run the files type "java frame" on the command promt in the appropriate directory.
Open the any off files provided(this version does not incorporate color and hence those 
files with color data will not be opened). The subdivide button will perform one round 
of subdivision.Redraw will display the previous topology. Usualy supports upto 4 round of subdivision 
depending on the size of the file